import global_action from './globals/global-action';
import sidebar_action from './sidebars/sidebar-action';

const rootAction = {
    global: global_action,
    sidebar: sidebar_action
}

export default rootAction;